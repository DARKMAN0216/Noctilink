{
  "name": "noctilink-client",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.node.json",
    "sync-story-data": "node tools/sync-story-data.mjs",
    "test": "npm run build && node dist-tests/tests/StoryEngine.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.0"
  }
}
